# SQL_more_queries

This project covers MySQL user management and privileges, table constraints
(NOT NULL, UNIQUE, PRIMARY KEY, FOREIGN KEY), and retrieving data from
multiple tables using subqueries, JOIN, and LEFT JOIN.
