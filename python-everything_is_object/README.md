# python-everything_is_object

This project explores how Python handles objects, references, mutability, and
aliasing under the hood — including `id()`, `type()`, small integer caching,
string interning, and how mutable vs immutable objects behave when passed to
functions.
