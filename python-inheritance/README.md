# python-inheritance

This project covers inheritance in Python 3: base/parent classes, subclasses,
multiple inheritance, method overriding, and built-ins like isinstance,
issubclass, type, and super.
