# python-network_1

This project covers fetching internet resources in Python 3 using both the
built-in urllib package and the requests package: GET/POST requests, reading
response headers, handling HTTP errors, working with JSON APIs, and using
Basic Authentication.
